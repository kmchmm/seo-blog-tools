import React from 'react';

const Steps = () => {
  //     const renderCard = (steps: {id: string, step: string, listItems?: string[]}) => {
  //         return   <div className="p-6 border rounded shadow max-w-3xl bg-white">
  //       <h2 className="text-center font-semibold text-xl mb-4">Steps For {mode === 'batchAnalysis' ? 'Batch' : 'Single'} Review</h2>
  //       <ol className="list-decimal pl-6 space-y-3">
  //         {steps.map(({ id, step, listItem }) => (
  //           <li key={id}>
  //             <div>{highlightText(step)}</div>
  //             {listItem && (
  //               <ul className="list-disc pl-6 mt-2 space-y-1">
  //                 {listItem.map((item, i) => (
  //                   <li key={i}>{item}</li>
  //                 ))}
  //               </ul>
  //             )}
  //           </li>
  //         ))}
  //       </ol>
  //     </div>
  // }

  return <div className="rounded-md border-gray-200 border">Steps</div>;
};

export default Steps;
